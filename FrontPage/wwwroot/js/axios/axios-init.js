//const AUTH_TOKEN = 'Bearer ' + MApp.getCookie('Authorization')

//window.instance = axios.create({
//    baseURL: `${mappUrls.gateway}`
//});

//window.instance.defaults.headers.common['Authorization'] = AUTH_TOKEN;